apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "employee.fullname" . }}-config
  labels:
    {{- include "employee.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
